{{#skipFormatter}}
    <!-- @formatter:off -->
{{/skipFormatter}}
package {{packageGroupName}}.{{moduleCommonDtos}}.{{entityNameLower}};

{{#imports}}
import {{.}};
{{/imports}}
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.ToString;

@Getter
@ToString
@AllArgsConstructor
public class {{entityName}}SearchCondition {
    {{#fields}}
    private final {{type}} {{name}};
    {{/fields}}
}
