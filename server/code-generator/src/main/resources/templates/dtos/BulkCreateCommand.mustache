{{#skipFormatter}}
    <!-- @formatter:off -->
{{/skipFormatter}}
package {{packageGroupName}}.{{moduleCommonDtos}}.{{entityNameLower}};

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.ToString;

import java.util.List;

@Getter
@ToString
@AllArgsConstructor
public class {{entityName}}BulkCreateCommand {
    private final List<{{entityName}}CreateCommand> commandList;
}
