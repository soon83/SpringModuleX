{{#skipFormatter}}
    <!-- @formatter:off -->
{{/skipFormatter}}
package {{packageGroupName}}.{{moduleServerInterfaces}}.{{entityNameLower}};

{{#imports}}
import {{.}};
{{/imports}}
import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.ToString;

@Getter
@ToString
@AllArgsConstructor
public class {{entityName}}InfoResponse {
    {{#fields}}
    private {{type}} {{name}};{{{newline}}}
    {{/fields}}

    {{#fields}}
    {{#isEnum}}
    public String get{{capitalizedName}}Title() {
        return {{name}}.getTitle();
    }
    {{/isEnum}}
    {{/fields}}
}
